# Copy to .env. NEVER commit real secrets (ADR-0001: secrets in env only).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/resolution
STRIPE_API_KEY=sk_test_xxx
AGENT_BASE_URL=http://localhost:8000
ANTHROPIC_API_KEY=sk-ant-xxx
